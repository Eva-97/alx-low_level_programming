#include <stdio.h>

/**
 * main - Alphabet game
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int char = 'a';

	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
