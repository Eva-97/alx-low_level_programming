#include "main.h"

/**
 * _islower - checks for lowercase character
 * @s: the character to check
 * Return: 1 if s is lowercase, 0 otherwise
 */
int _islower(int s)
{
	return (s >= 'a' && s <= 'z');
}
